[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"


[project]
name = "jsonfold"
dynamic = ["version"]
description = "Indented JSON, but with folded whitespace to have more data on a single line."
readme = "README.md"
requires-python = ">= 3.8"
authors = [
  {name = "Stefaan Lippens", email = "soxofaan@gmail.com"},
]
license = "MIT"
license-files = ["LICENSE.txt"]
keywords = ["JSON", "serialization"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
  "License :: OSI Approved :: MIT License",
  "Topic :: File Formats :: JSON",
  "Topic :: Utilities",
]


[project.urls]
Repository = "https://github.com/soxofaan/jsonfold"
Changelog = "https://github.com/soxofaan/jsonfold/blob/main/CHANGELOG.md"


[project.optional-dependencies]
dev = [
    "pytest>=8.3",
]


[project.scripts]
jsonfold = "jsonfold.cli:main"



[tool.hatch.build.targets.wheel]
packages = ["src/jsonfold"]

[tool.hatch.build.targets.sdist]
include = [
  "src/",
  "tests/",
]

[tool.hatch.version]
path = "src/jsonfold/__init__.py"


[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
